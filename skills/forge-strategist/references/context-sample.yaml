# Forge operator context — created by forge-planner on first engagement
# Read/written by forge-planner and forge-assembler
# NEVER commit this file to any git repo (INV-005)

operator:
  tools:
    - nmap
    - nuclei
    - httpx
    - subfinder
  infrastructure:
    automation: n8n  # or: tines, soar platform name
    datasets:
      - path: /data/datasets/censys
        description: "Censys IPv4 snapshot"
    custom_wordlists:
      - path: ~/wordlists/custom-subdomains.txt
        description: "Custom subdomain wordlist"
  preferences:
    workflow_style: methodical  # or: fast, thorough
    reporting_format: markdown
  skill_level: senior  # inferred by planner, not self-reported

campaigns: []  # populated by forge-assembler after each campaign
